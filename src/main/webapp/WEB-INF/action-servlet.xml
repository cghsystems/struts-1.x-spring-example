<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <description>
        Required to configure the org.springframework.web.struts.DelegatingActionProxy defined in struts-config.xml.
        This is where the action mapping should live. Note. Any beans that live here can access beans declared in
        applicationContext.xml this is because all DelegatingActionProxy contexts inherit contexts declared in a
        ContextLoaderListener
    </description>

    <bean name="/test" class="com.intetech.example.spring.struts.ExampleAction"/>

</beans>